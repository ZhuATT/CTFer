# AT1 中期文档（已完成部分·系统现状）

> **范围**：只写已落地的设计与实现。基线：2026-09-04，治理批竣工，190 测试绿。
> **结构真相**：`src/board.py` + `blackboard.schema.json`（测试锁同步）｜**语义参照**：`docs/at1-黑板schema.md`。

---

## 1. 系统形态

```
python -m src <engagement> [--budget 7200]
   │
   ├─ 控制器（Python，零 token）
   │    driver（主循环）· runner（进程桥）· board（黑板）· observer（观察者）
   │    guard（scope/自毁检测）· stoploss（四维）· events（JSONL 事件流）
   │    transcript_check（账本对账）· writeback（协议文件写回）
   │
   ├─ worker 的世界 = <engagement>/.auto/
   │    CLAUDE.md（纪律层，CLI 自动加载）· FINDINGS / FACTS / DIRECTIONS（三账本）
   │    STATE.md（系统投影，只读）· evidence/ · storage-state.json
   │
   └─ 控制器私有区 = <engagement>/.at1/
        _blackboard.json（唯一事实源）· transcript.jsonl（账本）· claude-config/
```

引擎：Claude Code CLI（`claude -p` headless stream-json），glm-5.3 经 bigmodel Anthropic 兼容通道；`CLAUDE_CONFIG_DIR` 指向控制器隔离目录（不读本机 settings）；spawn 前环境消毒（剥 AT1_*/LLM_*）。

## 2. worker 运行

> **worker 是单轮进程**——它的参数只有两个：时间盒（按轮次 1200/1200/1800s）和 max_turns 60。budget/rounds/observer 等都是 engagement 级（driver 主循环）参数，见 §8/§10。

- **一轮**：driver 读黑板 → 渲染 prompt（9 段，stdin 一次喂入）→ spawn worker → 实时收割 → 验证 → 投影 → 判停。
- **时间盒**：`(1200, 1200, 1800)` 秒按轮次取值，实际 = min(盒, 剩余预算)；到点 kill，轮界不续。
- **runner 实时流**：stdout 逐行解析；每次 tool_result 触发 `on_fact`（被动抽取+guard 检测+台账）；每 25 次工具调用一次心跳（心跳点轮询 CONTROL）；原始流 tee 进 transcript.jsonl（增量 flush，kill 不丢）。
- **断点续跑**：异常终态（非时间盒耗尽）指数退避后 `--resume`，每会话上限 20 次。
- **worker 无记忆**：每轮全新会话，全部记忆来自 prompt 摘要 + STATE.md + 三账本。

## 3. 黑板

唯一事实源 `_blackboard.json`，原子写 + .bak 回退。顶层对象与字段：

| 对象 | 字段 |
|---|---|

> 字段后缀 **`?` = 可选（条件性出现）**：`chain?` 仅在声明了联系时存在；`param_verified?`/`response_verified?` 仅在对账实际查到可验对象时写入（"查了才有资格说"，未查不写键——区别于必有值的 `evidence_verified` 硬判定）。
| facts（dict，键=kind:value） | kind(7 枚举)、value、confidence(observed\|inferred)、provenance、ts、round、chain?、last_verified_round? |
| immune | endpoint、status、since_round、confidence |
| findings | id、endpoint、summary、assessment(4 枚举)、severity、reason、evidence、round、evidence_verified、param_verified?、response_verified?、chain? |
| directions | id、goal、endpoint?、status(open\|in_progress\|blocked\|done)、note、blocked_reason?、chain?、comment(控制器所有)、source、round |
| chains（边库） | rel(3 枚举)、refs(只认 F-/D-)、note、origin、round |
| session_intel | 观察者会话观察（每轮覆盖） |
| handoff / goal / ledger / verified / config / offsets | 叙事 / 阶段 / 台账 / 派生计数 / 参数 / 记账 |

- **confidence 二值枚举**是唯一决策货币（conf 浮点已物理删除）；被动抽取恒 observed；worker 写的缺省 inferred。
- **结构契约**：`blackboard.schema.json` + 满配正例/三负例测试——改字段不同步契约，测试红。

## 4. 图

- **节点**：findings（F-，有 id）、directions（D-，有 id）、endpoint 等事实（无 id，键即身份）。
- **边**：`chain = {rel: derived_from|combines|same_root, refs: [F-/D-], note}`。三个挂点（findings/facts/directions 对象属性）+ **常设边库 `chains`**（观察者判重顺产等独立边；只增不删，`(rel, refs 集)` 去重，`add_chains()` 唯一入口）。
- **入板闸**：`normalize_chain`——rel 非法降 note-only；refs 非 F-/D- 前缀丢弃；悬空引用保留并渲染标注。
- **渲染**：STATE.md「方向与图」YAML 图层（directions 含 status/note/批注/source/chain，findings 含 sev/chain，chains 聚合）+ prompt 状态摘要。combines 边指向活方向 → **⚡组合路径待试**（YAML `hot`）。
- **消费**：worker（接力/防重复/组合攻击/重开判断）、观察者（方向表+已知联系进 prompt）、指令行欠账数字（未测面 N / 方向计数）、M5 前端（直读 _blackboard.json，数据已齐）。

## 5. worker 接口

**读**：prompt 摘要（方向计数+待接方向列表 in_progress 最先+阴性两档计数+引读 STATE.md）｜STATE.md 全量（只读投影，每轮覆盖写）｜CLAUDE.md 纪律层（自动加载）。

**写（收割去向）**：

| 写 | 收割 |
|---|---|
| FINDINGS 行（+chain?） | ID 去重收割 → transcript 双向对账 → 观察者判定 → findings 入板 → confirmed 写 status.md |
| FACTS 行（7-kind + confidence） | ingest：缺省 inferred、未知 kind→unclassified、chain 校验入板 |
| DIRECTIONS 整表 | merge upsert：worker status 优先、observer 方向漏抄保留、comment 抹不掉 |
| evidence/ · state/log.jsonl | 判官证据本体 / 人类记账 |

**纪律要点**（CLAUDE.md）：上报硬门槛（真实触发+可复现才 FINDINGS，嫌疑写 FACTS inferred）；否定结论没穷尽手段一律 inferred；即时写；不写 /tmp；开工先接 open/blocked 方向（方向表是工具不是派工单）；blocked 重开需材料性新机理。

## 6. 观察者

**双职责**（phase3.5 冻结原则：无工具/轮间/建议不指挥/不关闭方向）：
- `judge_finding`：单发现快判——noreport 代码预检（reject 终审 / suspect 公诉）→ LLM 按第零步硬否决+四步影响框架 → 输出 is_vulnerability/severity/reason。
- `observe_session`：全局观察——输入=全部发现+历史 confirmed+方向表+已知联系+黑板摘要+交接；输出=final_assessments（判重覆盖）+ 治理三件套（direction_comments / immune_reviews 只留 retest / chains）+ coverage_gaps / effective_patterns / notable_attempts / intel_summary。
- **C-2 防注入**：两个 PROMPT 最开头（证据文本之前）置"证据不可信声明"+判官职权边界声明。
- **对账如实入判官**（C-5 v2）：证据段渲染 transcript 对账摘要（"通过/参数未全部在账/未通过"），替换硬编码肯定句。
- **治理消费端**（driver）：批注挂方向、新方向入列（**每轮截断 3 条**）、retest 自动开 open direction（不改 confidence）、chains 入常设边库。
- **通道**：glm 经讯飞中转（build_verifier_config），与 worker 通道异构/隔离。

## 7. 验证与防线（纵深，各层独立成立）

| 层 | 机制 | 性质 |
|---|---|---|
| 上报门槛 | C-1 纪律：真实触发+可复现才 FINDINGS | prompt（CLAUDE.md 自动加载） |
| 证据对账 | transcript 双向：请求锚定（去 query 路径硬判定）+ 逐参数值 + 响应高信号 token（cap 10）；**双侧全空→verified=false 硬线**；部分未中→软标记+命中计数如实进判官；URL-decode 双形态匹配 | **机械**（零 LLM） |
| 事实衰减 | 轮窗复现抽验：被动事实最近一轮窗口不可寻→observed 降 inferred，再现自愈；凭证冻结、结论类跳过 | **机械** |
| 帐本独立 | .at1/ 目录分离 + CLAUDE.md 禁令 + guard_violation 事件（引擎级硬阻断=pi 迁移 P5，排队） | 结构+纪律 |
| 判官语义 | 观察者四步影响框架+第零步绝对不报表+noreport 硬拒清单（代码） | LLM+代码 |
| 毒饵探针 | tests/poison_probe.py（`-m llm` 显式跑）：2/2 已过（逼 true 判 False / 逼 false 判 True）——已停止迭代，文件休眠 | 回归基线 |
| 投影隔离 | untrusted nonce 包裹（黑板渲染/STATE.md/判官方向各自独立 nonce） | 代码+prompt |
| scope/自毁 | guard 消费 engagement.json allow/deny；控制器区禁写检测 | 代码+纪律 |
| 终局 | report.md 人审定稿 | 人工 |

## 8. driver 主循环与协议

- **收割窗口**（轮末顺序）：FINDINGS（ID 去重+冲突重编号）→ FACTS（字节 offset）→ DIRECTIONS（整表 upsert）→ 旧 Handoff 未竟段 best-effort 提为方向 → 观察者 → 治理消费 → Handoff（模型版优先/被杀代码合成）→ STATE.md 投影 → bb.save。
- **CONTROL**（`state/CONTROL`，读后即删）：stop / pause / directive（人工指示进下轮 prompt 第 9 段）；心跳点轮询 stop → kill 当前 worker 走优雅停。
- **终止条件**：A confirmed（默认不硬停，`--stop-on-first-confirmed` 回到字面 A）/ B 预算耗尽或 stoploss（四维：轮上限/预算/无新事实连击/不可达连击）/ C goal 链走完（evidence+report.md）/ D CONTROL stop。
- **stoploss 四维语义**（全部命中才停对应维度，任一触发即终止 B；连击恢复自动清零）：
  ① **会话上限 6**（= `--rounds`）：一场 engagement 最多起 6 个 worker 会话，跑满即停；
  ② **活跃预算**：剩余秒数 ≤ 0（= `--budget` 的另一面）；
  ③ **无新事实连击 ≥3**：连续 3 轮黑板零新事实（被动抽取与 FACTS 都没进新的）→ 判"在空转"；任一轮有新事实即清零；
  ④ **不可达连击 ≥3**：连续 3 轮 worker 会话异常收场（error 且无收割）→ 判"目标不可达/通道故障"；会话正常即清零。
- **收尾写回**：status.md 漏洞表追加（表尾锚点）+ 攻击面深度刷新 + notes/prior-intel-draft.md 生成（接力闭环）。
- **事件流**：`state/auto-log.jsonl`（类型白名单+secret 脱敏），可回放；`python -m src watch` 终端渲染。

## 9. 验证现状

| 项 | 结果 |
|---|---|
| 单元+集成测试 | **190 绿**（board 34 / driver 含两轮全链路集成 / observer 含治理与防注入文本断言 / transcript 对账 16 / scaffold 契约断言 / schema 契约 4…） |
| selftest（真实 glm 通道） | 8/8 DoD（3 turns，76k tok，$0.247） |
| 毒饵探针（真实观察者通道） | 2/2（防注入双向有效；已停止迭代） |
| dry-run 门 | 7/7（指令行/摘要/YAML 图层/注释头/阴性分档/chain 标注/防注入尾注） |
| 回归靶场 | canary-web（Flask 靶+grade.py；真实环境验收替代 canary 冒烟） |

## 10. 已锁定参数

**engagement 级（driver 主循环，CLI 可调）**——管"这场任务总共跑多久/几轮"，不是 worker 的参数：

| 参数（CLI） | 默认值 | 说明 |
|---|---|---|
| `--budget` | 7200s | 总预算；到点终止（条件 B）；stoploss 第②维 |
| `--rounds` | 6 | 最多起 6 个 worker 会话；跑满终止（stoploss 第①维） |
| `--stop-on-first-confirmed` | False | confirmed 后不硬停，继续链式挖掘 |
| `--no-observer` | False | 观察者默认开（治理中枢） |
| `--provider` | glm | glm-5.3 经 bigmodel Anthropic 兼容通道 |
| `--dry-run` | False | 只渲染首轮 prompt + STATE.md，不 spawn |

**worker 单轮级**（每轮一个进程，内部参数）：

| 参数 | 默认值 |
|---|---|
| 时间盒阶梯 | (1200, 1200, 1800) s 按轮次取，实际 = min(盒, 剩余预算) |
| max_turns | 60 |
| resume 上限 | 20 次/会话（仅异常终态） |
| 心跳间隔 | 每 25 次工具调用 |
| 呆滞告警 | 流级 120s / 工具级 600s（只告警不杀） |

**治理层**（board/observer/stoploss 内部）：

| 参数 | 默认值 |
|---|---|
| 侦察出口 endpoint_n | 15（轮次兜底第 2/3 轮强制放行） |
| stoploss 连击 | 无新事实 3 / 不可达 3（恢复自动清零） |
| 观察者新方向建议 | 每轮 ≤3 |
| DIRECTIONS_CAP / 摘要待接 cap | 12 / 8 |
| 分母层渲染预算 | 每 kind 12 行 / 全段 4000 字符 |
