# AT1 v3 批 3 施工执行单

> **上游**:`at1-v3批3-计划.md`(范围已闭:七件,T4.2 已裁取消,四决策点全落)。
> **文本定稿**:`prompt-全文汇编.md` §3.1(手册)/§3.2(FORMATS)/§3.3({env_bg})——施工原样落位,已预置于 scaffolding/。
> **基线**:`6c3d02a`(批 2,223 绿)。**纪律**:中间态可红(红线 gate commit);每卡自检;做完全绿回写等指示;**批 3 完成即解锁真轮**。
> **状态:已竣工(09-19,卡 1-5 全勾,223 绿,未 commit 待指示)**

---

## 前置清单

- [x] `git status` 干净、`git log -1` = 6c3d02a
- [x] `.venv/Scripts/python.exe -m pytest -q` = 223 passed
- [x] scaffolding/WORKER-CLAUDE.md 与 FORMATS.md **已按定稿落位**(审阅通过)——施工不再动这两文件;开工时 test_scaffold 3 条 v2 断言红(预期中间态,卡 2 修复)

---

## 施工卡

### 卡 1 围栏统一(一致性锁的前置)

- 动:汇编 §3.1/§3.2 的代码围栏统一为与落位文件**逐字节相同**的形态(现 Handoff 行是 4 空格缩进、落位文件是 ``` 围栏——不统一则逐字锁永远红);外层改四反引号嵌套
- 自检:diff <(汇编提取段) scaffolding/ 两文件 = 零差异(手工 diff 核对)

### 卡 2 scaffold.py 翻新(T3.3 全部)+ Guard 接线

- 动:
  1. `load_engagement`:fail-fast 只剩 target;scope/mission 可选(mission 读入即弃——字段废除)
  2. 新增 `seed_engagement(eng, bb)`:target/goal/hint **空则播**进 bookkeeping(**续跑不覆盖**);scope **每次覆盖**(边界以最新委托为准)
  3. `_render_worker_claude(bb)`:改从 **bookkeeping** 取槽值({target}/{hint};{hint} 空→"（无）");{env_bg}=汇编 §3.3 文本常量
  4. `expand(root, eng, bb, skills_src)`:渲染走 bb;**DIRECTIONS 预创建删+遗留旧 DIRECTIONS 文件清理**;`.auto/reports/` 预建;**src标准/ 拷进 .auto/src标准/**;账本注释头改指 FORMATS(删 DIRECTIONS 头)
  5. `Guard` 接线:`from_engagement(eng)` → 改读 bookkeeping.scope(签名以 guard.py 现状为准);**scope 缺→目标拦截停用(禁区/自毁保留)**
- 自检:`tests/test_scaffold.py` 全重写——三槽渲染/{hint} 空"（无）"/播种空则播+续跑不覆盖/scope 每次覆盖+缺失停用/DIRECTIONS 删+清理/reports+src标准 落位/账本头;**一致性锁 3 断言**:①模板三槽齐全 ②渲染产物零残留 `{` ③汇编 §3.1/§3.2 提取段与 scaffolding 两文件逐字一致
- 自检:`pytest tests/test_scaffold.py -q` 绿

### 卡 3 driver/harvest 适配(播种顺序+guard+mission 废除+chain 三动词)

- 动:`run_engagement` 重排——`bb = Blackboard(...)` 提到 `expand` **之前**;`seed_engagement(eng, bb)` → `expand(root, eng, bb, skills_src)`;guard 改读 bb.bookkeeping.scope;`scope` 缺→`ev.emit("scope_missing")`(events 白名单注册);STATE 投影与调用去 mission;dry-run 分支同步
- 动:`harvest._declare_chain` 扩三动词(配合 FORMATS 教学,批 1 只接线 derived_from/same_root):`derived_from`=本条→ref;`sources`=本条(F/T)→ref(D-xxx);`yields`=**ref(D-xxx)→本条**(方向约定相反,翻边);same_root 移除出 worker 声明表(FORMATS 已教"判重归观察者")
- 自检:`tests/test_driver.py` 适配——mission 断言删;新增 goal 播种(空则播/续跑不覆盖)、scope_missing 事件两测;`tests/test_harvest.py` 加 chain 三动词方向断言(sources F→D/yields D→F/翻边不写反)
- 自检:`pytest tests/test_driver.py tests/test_events.py tests/test_harvest.py -q` 绿

### 卡 4 prompt.py 三块终态(T4.1/T4.3)

- 动:`render_round_prompt` 重写为**恰三块**:
  - 【引导】bookkeeping.guide 存在则整块注入(原文,块缺席=无事)
  - 【运行提示】directive(=hints 聚合/CONTROL directive,批 2 已喂;空则整块消失,N3-2)
  - 【简报】轮次/预算/时间盒/身份 + 判层指引("判层规则见 CLAUDE.md §2,格式见 FORMATS.md")——**删"发现即提交"旧教义**
  - 删残版【状态摘要】【提示】(路由已裁);`board.summarize` **方法保留**(P5 前观察者快照还在用),仅退出 stdin
- 自检:`tests/test_prompt.py` 全重写——恰三块/guide 注入/空块消失/判层指路/零"发现即提交"/零摘要零路由
- 自检:`pytest tests/test_prompt.py -q` 绿

### 卡 5 收尾验收

- [x] `.venv/Scripts/python.exe -m pytest -q` 全量绿(223 passed)
- [x] 残留 grep:零活引用
- [x] dry-run 冒烟(usc-fresh 副本+hint/goal-set):hint 进【运行提示】/CLAUDE.md 零残留槽/src标准 五件拷贝/FORMATS 落位
- [x] 回写:批 3 计划回写区 + 总纲横幅 + P3/P4 任务表 ✅ 逐件
- [x] commit 描述拟好,已提交推送(用户 09-19 指示)

---

## 明确不做(本批边界)

- OBSERVER-MANUAL/观察者 -p 重写/七类行激活 = 批 4(P5)——OBSERVER 文件钩子批 2 已预铺
- STATE.md 防注入 = A20 挂起
- server/API = M5(总纲 M5 待办已记:薄壳+token+并发写协调)
- 真轮验收 = 批 3 完成后解锁(本批竣工即可跑)
