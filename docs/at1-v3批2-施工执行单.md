# AT1 v3 批 2 施工执行单

> **上游**:`at1-v3批2-收割投影终态落地计划.md`(范围/决策点已定稿)——本单是它的执行顺序,只回答"先干什么、每步动什么、怎么自检"。
> **基线**:`45d7491`(203 绿)。**纪律**:中间态可以红(红线只 gate commit);每卡自检=跑该卡指定的测试;全量绿后回写、等指示 commit;observer.py 本体不动;批间不跑真轮。
> **状态:已竣工(09-18,卡 1-10 全勾,223 绿,未 commit 待指示)**

---

## 前置清单

- [x] `git status` 干净、`git log -1` = 45d7491
- [x] `.venv/Scripts/python.exe -m pytest -q` = 203 passed
- [x] 依赖顺序确认:卡 3 依赖卡 1(guide 键);卡 4 依赖卡 2;其余独立,但按卡号走

---

## 施工卡

### 卡 1 schema 先行:bookkeeping 加 guide 键

- 动:`blackboard.schema.json` bookkeeping.properties 加 `"guide": {object, required:[round,text], additionalProperties:false}`;`tests/test_schema_contract.py` 满配 `_full_board()` 补 guide 行
- 自检:`pytest tests/test_schema_contract.py -q` 绿

### 卡 2 runner STOP 抽取(四处镜像)

- 动:`src/runner.py`——①`_STOP_RX`(仿 `_HANDOFF_RX`:27)②`extract_stop`(仿 :60)③`AgentResult.stop_text: str = ""`(:88)④`harvest_stop()`(:267,同 harvest_handoff 的逆序扫描)⑤`to_result()`(:277)接线
- 自检:`tests/test_runner.py` 加 `test_extract_stop_basic/empty`(有标记/无标记/多标记取最后有效)
- 自检:`pytest tests/test_runner.py -q` 绿

### 卡 3 七类行解析器(新文件)

- 动:新建 `src/observer_harvest.py`——`apply_observer_lines(bb, lines, *, round, rejected_ids) -> {counts, rejects}`,七类行处置按落地计划 §二件 2 表(verdict 硬拒不受理/same_root 正主规则+机械 dismiss/supersedes/comment/intent+spawns/intel/guide→bookkeeping.guide;坏行进 rejects)
- 自检:新建 `tests/test_observer_harvest.py`——七类行逐类+正主 primary 正常/**primary=src 翻转**/缺失回退两例+隔离区+硬拒不受理+supersedes 仅 confirmed 可迁
- 自检:`pytest tests/test_observer_harvest.py -q` 绿

### 卡 4 driver STOP 接线

- 动:`src/driver.py` 终止判定区(642 行处)插入:`parse_stop(res.stop_text)` 有效→`worker-stop(C)`+`worker_stop` 事件+break;无效→`stop_invalid` 事件继续
- 自检:`tests/test_driver.py` 加两例(mock runner 带不 `res.stop_text` 字段的构造:有效 Stop 两轮停/无效 Stop 继续跑+事件断言)
- 自检:`pytest tests/test_driver.py -q` 绿

### 卡 5 noreport 检察官前置

- 动:`src/driver.py` `_harvest_findings` 之后:新发现逐条 `noreport.check(row, evidence_text)`→reject 建节点即 dismissed(`reason="硬拒[category] …"`)+`hard_rejected` 事件+从 `new_findings` 剔除;`rejected_ids` 传给卡 3 的收割调用
- 自检:`tests/test_driver.py` 加:硬拒样本(summary 含 sourcemap)→图内 dismissed+观察者输入不含;凭证形状样本→放行
- 自检:`pytest tests/test_driver.py tests/test_noreport.py -q` 绿

### 卡 6 STATE.md 六节终态+两刷

- 动:`src/driver.py` `_render_state_projection` 重写(计数行第一行/六节/方向 cap 12+溢出行/报告索引;任务概要的"人工已确认发现"取数=parse_status_findings(status.md 漏洞表),与图内 confirmed 分列);两刷:刷 1 插收割后(~564 行后)、刷 2 保持现 639 行处;dry-run 只刷 v1
- 自检:`tests/test_render_consistency.py` 改(计数行↔六节互证、两刷差量);`tests/test_driver.py` dry-run 用例更新
- 自检:`pytest tests/test_render_consistency.py tests/test_driver.py -q` 绿

### 卡 7 events 扩展+watch

- 动:driver 各卡已埋的事件这里收口核对(`worker_stop/stop_invalid/resume/guide_injected/observer_parse_fail/hard_rejected/reports_promoted`);`src/__main__.py` watch 渲染表加分支;`resume` 事件=run 启动时图非空
- 自检:`tests/test_events.py` 加新事件断言;`pytest tests/test_events.py tests/test_cli.py -q` 绿

### 卡 8 hints 队列+CLI 三壳

- 动:`src/driver.py` 轮间轮询区(504 行区)加 hints 收割(`diff_new_lines`+`offsets["hints"]`)→聚合拼进 directive_next;`src/__main__.py` 加子命令:`hint`(append jsonl)/`goal-set`(开板 set_goal→save)/`stop`(写 state/CONTROL)/`resume`(提示指向 run)
- 自检:`tests/test_cli.py` 加三壳用例(tmp engagement);`tests/test_driver.py` 加 hints 收割用例(追加→下轮 prompt 人工指示可见→偏移推进不重复)
- 自检:`pytest tests/test_cli.py tests/test_driver.py -q` 绿

### 卡 9 usc 真账本回放+报告晋升

- 动:`tests/test_driver.py` 加 usc-fresh 真账本收割回放(`../../../engagements/usc-fresh/.auto/{FINDINGS,FACTS}` 拷 tmp→收割→断言入图数;**skipif 不存在**);`src/writeback.py` 加 `promote_reports(root, bb)`(confirmed 且 `.auto/reports/F-xxx.md` 存在→copy 到 `reports\`+`reports_promoted` 事件)+单测(tmp e2e)
- 自检:`pytest tests/test_driver.py tests/test_writeback.py -q` 绿(本机应见 usc 回放真跑)

### 卡 10 收尾验收

- [x] `.venv/Scripts/python.exe -m pytest -q` 全量绿(**223 passed**,203 起只增)
- [x] 死引用 grep:仅剩 prompt.py docstring 历史描述,零活引用
- [x] dry-run 冒烟(usc-fresh 临时副本):计数行+八节目检;`hint`/`goal-set` CLI 后 prompt 可见
- [x] 回写:批 2 落地计划 §六回写区 + 总纲进度横幅 + 总纲 P2 任务表 ✅09-18 逐件
- [x] commit 描述拟好,已随批 2 提交推送(用户 09-18 指示)

---

## 明确不做(本批边界)

- observer.py 本体重写(-p 同构/OBSERVER 产出)= P5
- stdin 三块终态(【引导】/【运行提示】)/FORMATS.md/worker 新模板 = 批 3(P3+P4)
- 报告晋升的真实激活(目录与报告制度)= 批 3 建,批 2 只交付函数
- 真实轮次行为验收 = 批 3 之后
