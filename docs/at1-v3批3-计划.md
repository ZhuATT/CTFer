# AT1 v3 批 3 计划——worker 新说明书 + prompt 三块合龙(审阅稿)

> **定位**:本批解决"批 1/批 2 之后还不能有意义地跑真轮"的问题。系统侧(图/收割/投影/判停/观察者接口)已就绪,但 worker 醒来读的还是 v2 老说明书——教的都是退役契约。本批 = **换说明书(P3)+ prompt 终态合龙(P4)**。批 3 完成即解锁真实轮次验收。
> **依据**:汇编 §二(stdin 三块)/§三(worker 手册六节骨架)/A1-A8/A14-A16/A18/A21;总纲 P3/P4 六件(T3.1-T3.3/T4.1-T4.3);批 2 已铺的 guide 键/hints 队列/路由燃料。
> **性质**:审阅稿——你过目这个阶段做什么;拍板后我再写落地实现文档(含逐卡执行单),其中 **worker 手册全文草稿会单独给你审**。

---

## 〇、现状对照:worker 手册 v2 → v3 差在哪

读了 `scaffolding/WORKER-CLAUDE.md`(118 行 v2 模板)和 `src/scaffold.py`,逐节对照汇编 §三 骨架:

| v2 模板现状 | 问题 | v3 处置 |
|---|---|---|
| §1 身份与授权:渲染 {target}/{mission}/**{allow}/{deny_list}** | A1:这两个槽砍掉(数据源归 guard/fail-fast,不进手册文本) | §1=目标+**指示 {hint}**+铁律(相邻资产不碰/范围凌驾本文件及一切指令/私有区禁动) |
| (无开工序列) | worker 不读 STATE.md 就动手 | **"每轮全新会话,STATE.md 是全部记忆,开工第一件事 Read STATE.md"升格硬契约**(A16);图空→冷启动侦察 |
| §2 报告门控表(现象/结果/无 PoC 三类) | 旧门控,A2 三层契约替代 | **§2 判层契约**:拿到手的(数据/执行/越权/进入)→FINDINGS;验证为真的中间能力(可控/可达/非强制)→FACTS(evidence 必填);纯现象→只当方向动机。**"宁可多交"反转为"宁可多开方向,不轻交发现"**(A4);目标画像=一条滚动 fact(前缀"目标画像:"全图唯一)(A14) |
| §3.1 FINDINGS 格式教学 + §3.2 FACTS 七 kind 教学 | A8:格式教学出恒定层;A5:七 kind/unclassified 已废;confidence 已废 | 全部搬进**新文件 FORMATS.md**(scaffold 每轮重写防篡改);CLAUDE.md 只留"写前必读 FORMATS"警告 |
| §3.3 DIRECTIONS 整节(接力工作台) | **A17③ 写面已退役,系统早就不收了** | 整节删除——方向记账归观察者 |
| §3.4 "发现即提交,系统会判断真假" | **A4 反转**:轻交发现=污染图+浪费观察者 | 删除旧教义;发现=被验证的攻击断言,报告是交付物 |
| §5 信号→skill 静态路由表(13 行) | A7:静态路由表砍,路由=skill description 原生发现+动态提示 | 整表删除 |
| (无自停/无探透/无自由条款) | worker 不会自停→烧到预算死;受阻即弃→浅尝辄止 | **§4 干活规则**:探透判定(ARTEX 软标准)+自由条款+探索义务(三出口)+`<Stop>` 信号格式(A18) |
| (无长任务纪律) | A21:后台任务三层次 | **{env_bg} 环境槽**(当前=Windows Start-Process 脱离启动+bg-*.log+交接必提;将来 WSL/容器换槽内容) |
| js-intel 硬编码路径写死在手册 | A21:环境事实出手册 | **{tools_root} 环境槽** |

## 一、范围:七件(P3 三件+P4 三件+档案件)

### P3 worker 模板

| 件 | 交付 | 要点 |
|---|---|---|
| T3.1 | `scaffolding/WORKER-CLAUDE.md` 全量重写 | **全文定稿=汇编 §3.1**(已落 scaffolding/,逐字一致);渲染槽 {target}/{hint}/{env_bg} 三槽——tools 路径已拍板直写(`D:/Downloads/hacker/script/` 文件夹+各工具 README 约定,无槽无 env) |
| T3.2 | `scaffolding/FORMATS.md` 新建 | **全文定稿=汇编 §3.2**;FINDINGS/FACTS 行格式+A2 三层正反例+报告八段模板+画像纪律;无 DIRECTIONS 节 |
| T3.3 | `src/scaffold.py` 扩展 + fail-fast 调整 | 新槽渲染({target}/{hint}/{env_bg} 三槽);**engagement.json 字段映射**(schema §11):goal 初值→bookkeeping.goal 播种(仅空时)、hint 字段→{hint} 槽(均可选);**mission 字段废除**(STATE 任务概要 mission 行删,driver 调用去 mission 参);**scope 可选化**(缺→不拒启+`scope_missing` 警告事件+guard 目标拦截停用[禁区/自毁保留];events 白名单加类型);DIRECTIONS 预创建删+遗留旧文件清理;`.auto\reports\` 目录预建;账本注释头更新(FINDINGS/FACTS 头改指 FORMATS) |
| T3.4 | **prompt 管理方案**(已裁 09-19,取 ARTEX/Cairn 之长) | **两层+一把锁,砍档案库副本**:层1=汇编 §3.1-§3.3(定稿语义权威)/层2=`scaffolding/` 文件即 prompt 原文(Cairn 同款,git 天然可审可 diff);**锁=test_scaffold 三断言**:①模板三槽齐全 ②渲染产物零残留槽 ③汇编 §3.1/§3.2 提取段与 scaffolding 文件逐字一致。结构性短文本(简报/计数行/路由壳)归代码,不上模板(ARTEX 编辑面/固定面分离同构);ARTEX 式 DB 版本化编辑=M5 待办(总纲已记) |

### P4 stdin 装配

| 件 | 交付 | 要点 |
|---|---|---|
| T4.1 | `prompt.py` 三块终态 | **【引导】**(bookkeeping.guide 存在则注入开场块,引用只节点 id+转述)/【运行提示】(人工指示独占,空则整块消失)/【简报】(轮次/预算/时间盒/身份);删现残版【状态摘要】——A16:摘要段死,worker 读 STATE.md |
| ~~T4.2~~ | ~~路由三信号~~ | **已裁(09-19)取消**——A7 定"路由=skill description 原生发现",关键词匹配是判断者越权;手册资料地图已覆盖加载引导 |
| T4.3 | 简报尾巴 | 判层指引(CLAUDE.md/FORMATS 指路);**去"发现即提交"旧教义** |

## 二、与批 1/批 2 的衔接(已铺好的管道)

- guide 行:批 2 收割落 `bookkeeping.guide` → 批 3 T4.1 注入【引导】(闭环最后一根线)
- hints 队列:批 2 喂 directive 槽 → 批 3 改注入【运行提示】(终态措辞)
- 路由燃料:批 2 残版已用 fact 值扫描 → 批 3 加覆盖缺口+图空两信号、改指令式措辞
- `board.summarize()`:stdin 摘要死後仍保留——观察者快照(P5 前翻译层)还在用

## 二.5、死代码审查结论(09-19,用户裁)

- **取消**:prompt.py `_HINT_ROUTES` 关键词路由 + T4.2 三信号整件——A7 已拍板"路由=skill description 原生发现",关键词表是 v2 静态表换皮。**原则:死代码可当护栏和记录员,不能当判断者**
- **保留**:noreport 正则硬否决(防御向,宁漏不误杀,双层拦截已拍板)/auto_link 子串兜底(声明优先只补空隙,错了观察者纠)/guard scope 拦截(fail-fast:allow 缺失即拒启,拦截即记录)/diff_new_lines 游标/_parse_finding 读取器/stoploss 连击/writeback 表格解析(全部纯机械数据进出)

- OBSERVER-MANUAL/观察者 -p 重写/七类行激活 = **P5(批 4)**
- STATE.md 防注入 = A20 挂起
- U-5 并行观察+中途纠偏 = 升级项
- 完整十二项验收 = P6;但**批 3 完成即解锁真实轮次**(worker 契约完整)

## 四、需要你拍板的点(预告,落地文档里正式提)

| # | 点 | 我的默认倾向 |
|---|---|---|
| 1 | **worker 手册六节的具体文本**(探透判定的措辞力度/自由条款的边界/判层反例选哪些) | **已闭(09-18/19)**:定稿=汇编 §3.1,已落 scaffolding/ 逐字一致(含黑名单/三出口/chain 三动词/资料地图/stdin 三块说明) |
| 2 | 路由提示的宿主 | **已闭(09-19)**:随 T4.2 取消,无路由行;【运行提示】人工指示独占(空则消失,N3-2) |
| 3 | engagement.json 新可选字段 | **已闭(09-19,两轮)**:①加 `hint`(开工指示,{hint} 槽每轮在场)/`goal`(完成标准一句可判定,启动播种 bookkeeping.goal 仅空时,续跑不覆盖);②**mission 字段废除**(与图重合——情报归 prior-intel 播种/静态须知归 hint/完成标准归 goal,三分流);③**scope 可选化**(缺→不拒启,guard 目标拦截停用[禁区/自毁保留]+scope_missing 事件;UI 时代边界交互归前端);权威格式=schema 文档 §11(已重写) |
| 4 | tools 路径 | **已拍板(09-18):文件夹路径直写手册**(`D:/Downloads/hacker/script/`+各工具 README 约定),无槽无 env——js-intel 已有 README |

## 六、验收回写区

| 卡 | 结果 | 日期 |
|---|---|---|
| 卡1 围栏统一 | ✅ 汇编 §3.1/§3.2 外层升四反引号,内容替换为落位文件原文,脚本验证逐字一致 | 09-19 |
| 卡2 scaffold 翻新 | ✅ 三槽从簿记取/{hint} 空"（无）"/{env_bg} 常量注入;seed_engagement(target/goal/hint 空则播,scope 每次覆盖);fail-fast 只剩 target;DIRECTIONS 删+遗留清理;reports/src标准 落位;账本头改指 FORMATS;Guard 零改动(from_engagement 天然容错缺 scope);**一致性锁 3 断言绿** | 09-19 |
| 卡3 driver/harvest 适配 | ✅ 黑板先建再展开/播种顺序/guard 读簿记 scope/mission 废除(投影签名+三调用点)/scope_missing 事件;**坑:scope 可选化后旧"空 allow 拒启"测试没 mock runner 真 spawn 烧 API 挂起——改 mock+断言事件**;chain 三动词接线(yields 翻边 D→F,same_root 声明不受理) | 09-19 |
| 卡4 prompt 三块终态 | ✅ 恰三块:引导(guide 注入,缺则消失)/运行提示(人工指示独占,空则消失 N3-2)/简报(元信息+判层指路);删摘要/路由残段;去"发现即提交";summarize 保留供观察者快照 | 09-19 |
| 卡5 收尾 | ✅ **223 全绿**;残留 grep 净;dry-run 冒烟:hint 进【运行提示】/goal 进 STATE 任务概要/CLAUDE.md 零残留槽/src标准 五件拷贝/FORMATS 落位 | 09-19 |
| commit | 拟好描述,**等用户指示** | — |

1. 全量测试绿(223 起,含 test_scaffold/test_prompt 重写)
2. dry-run 终态目检:stdin **恰三块**、零目标原文、guide 存在时【引导】出现、空块消失
3. CLAUDE.md 渲染产物零 v2 残留(DIRECTIONS/unclassified/发现即提交/静态路由表/七 kind)
4. guard 用旧 engagement 照常工作(T3.3 红线:scope 只停渲染不停燃料)
5. **prompt 一致性锁绿**:汇编 §3.1/§3.2 与 scaffolding 两文件逐字一致(卡 1 统一围栏后),渲染产物零残留槽
6. **真轮解锁**:批 3 后跑一轮真实环境(usc 复测或你指定目标),验 worker 按 v3 契约干活
