# AT1 中期真实环境测试文档（问题登记）

> **定位**：专记真实环境验收 run 中发现的问题/疣子/设计权衡观察。健康机制简录只做对照背景，发现问题为主。
> **首跑**：2026-09-14，engagement=`D:\Downloads\hacker\engagements\usc-fresh`（南华大学 fresh.usc.edu.cn / 172.20.9.26，内网外露节点）。
> **参数**：`python -m src run <engagement>`（budget 7200s 默认、观察者开、worker=glm-5.3-flash/bigmodel、观察者=deepseek-v4-flash/官方）。
> **基线 commit**：`2429712`（195 绿）。
> **终态**：3 轮后人工硬停（r1 end_turn 12min / r2 时间盒击杀 20min / r3 外部强杀 ~21min）。黑板终态 84 facts / 0 findings / 5 directions / 0 chains / 0 immune。
> **修复批（2026-09-14 当天，基于本 run）**：P-1~P-8 + P-11 全部修复（P-11 经两轮实机终验，**214 绿，未 commit**）；P-9 拍板非问题；P-10/P-12/O-1~O-5 观察结案。详见 §五/§六。

---

## 一、问题登记表

| # | 等级 | 问题 | 位置 | 现象/证据 | 根因 | 状态 |
|---|------|------|------|-----------|------|------|
| P-1 | 中 | `python -m src watch` 子命令崩溃 | `src/__main__.py` `cmd_watch`（json.loads 于 L374） | `NameError: json not defined`，事件流回放入口不可用 | 模块级无 `import json`；单测直接调 render 函数绕过 CLI 入口 | **已修复（2026-09-14）**：模块级补 `import json` 一行；真 CLI 验证通过（当初崩溃的原命令完整回放全 run 历史） |
| P-2 | 低 | watch 无测试覆盖（CLI 子命令路径零冒烟） | `tests/` | P-1 能活着 | 同上 | **已修复（2026-09-14）**：新增 `tests/test_cli.py` 三测——watch 完整 CLI 路径（含坏行容忍+渲染映射）、watch 缺文件 rc=2、run 拒启路径 rc=2；207 绿 |
| P-3 | **高（设计）** | 观察者触发面过窄：与"有发现"死绑 | `src/driver.py:401` `if new_findings and observer_on` | 真 run 三轮 0 FINDINGS（worker 守纪律走 FACTS）→ 观察者 0 次调用：判重/方向批注/coverage_gaps/chains 边/治理三件套全部静默，中期核心机制整个未被真实数据锻炼 | observe_session 本可每轮全局观察（输入方向表+事实，不依赖发现），被塞进 findings 触发条件 | **最小闭环已实现（2026-09-14，未 commit 待拍板）**：judge 留在 findings 分支 + 0-finding 轮独立调 observe_session + prompt 两句（空发现轮指令/批注防抖）+ 回归测试；196 绿；DeepSeek 真通道冒烟通过。缓：动作流水供料、【提示】回流 coverage_gaps——等下次真跑看输出质量再定 |
| P-4 | **高（设计）** | immune/阴性两档机制结构性悬空 | `src/driver.py:265`（add_immune 全仓唯一运行时调用方）+ `board.py:708-710/771/813`（渲染端） | 真 run immune=0：worker 按纪律只能把阴性结论写 inferred FACTS（unclassified），不进 immune；渲染出的"阴性记录/已否决模式/实测关闭 N/推断关闭 N"恒空。防重复实际只剩 DIRECTIONS done/blocked + worker 自觉 | immune 唯一来源=启动播种 status.md"已确认非漏洞"段；FACTS 阴性→immune 无升格通道；**且 CLAUDE.md 教 worker 把阴性写 FACTS，污染 FACTS 正向情报语义**（用户指出：FACTS 该只装真实发现和有用线索，参照 hxbai——正向进 knowledge/死路独立 deadend 存储由控制器提取） | **已实现（2026-09-14，未 commit 待拍板）**：① merge_directions 内 done→immune 机械升格（blocked 不算；同端点有 finding 不算——双向子串宽松匹配；档位恒 inferred；幂等）② prompt 三处收紧：FACTS 只装正向情报、阴性结论走 DIRECTIONS done+note（WORKER-CLAUDE.md §3.2/§3.3 + 账本注释头）③ 真实黑板回放验证：D-004/D-001 done 均正确入格，STATE.md 阴性段两档渲染活；198 绿 |
| P-5 | **高（防线）** | guard 控制器区检测存在 Windows 路径盲区 | `src/guard.py` `_ZONE_PATH_RX`（只匹配 `/`）+ `_WRITE_TOOL_HINT_RX`（无 .NET 静态写方法） | 实锤现场：worker `[IO.File]::AppendAllLines("..\state\log.jsonl",…)` 反斜杠写穿 state/ 禁区，**0 告警**。排查发现实锤命令穿的是**两道缝**：分隔符（`..\state\` 只认 `/`）+ 写动词表（`AppendAllLines` 不含任何已知写 token）。Write/Edit 工具路径参数本就有 `\`→`/` 归一，盲区仅在命令文本检查 | Unix 习惯正则 + 测试全用 `/` 形态造数据（P-2 同款：测试数据形态单一） | **已实现（2026-09-14，未 commit 待拍板）**：① `_ZONE_PATH_RX` 分隔符 `[/\\]` 双形态（含混合 `../state\`）② `_WRITE_TOOL_HINT_RX` 补 .NET 静态写方法（AppendAll*/WriteAll*/`[IO.File]`）③ 反斜杠四形态回归测试 + 阴性对照；199 绿。注：检测是录像不是门锁（设计如此）——真拦截在 pi 迁移 P5 |
| P-6 | 中 | CLAUDE.md 纪律层自相矛盾 | `scaffolding/WORKER-CLAUDE.md` §3（L38：每次主动测试 append `../state/log.jsonl`） vs §1（state/ 禁止读写）；**排查又发现第二处**：`prompt.py` 侦察手册第 4 步同样指使写 `state/log.jsonl`，还让 worker 在 `status.md` 攻击面表加行（同属禁区，且 writeback.refresh_surface_depth 本就机械维护该表） | worker 选择照 §3 写（台账 8 条），叠加 P-5 后 guard 全程沉默；P-5 修好后会反向产生假警报（诚实记账每笔一条 controller_zone 事件，真越界被淹没——狼来了），且 worker 被自己的手册训练成"禁区可以进" | 模板两段不同时期各写各的（§3 抄自外层手动 engagement 协议，路径原样带入）；没人对过 | **已实现（2026-09-14，方案 A 拍板，未 commit）**：① 纪律层/侦察手册台账改当前目录 `log.jsonl`（worker 世界），侦察手册撤掉 status.md 手写指令 ② `writeback.sync_human_ledger`：收尾由控制器把 `.auto/log.jsonl` 去重追加到协议位置 `state/log.jsonl`（外层协议兼容 + 续跑幂等）③ driver 收尾接线 + `ledger_synced` 事件登记 + 两测试（搬运去重/契约回归）；201 绿。禁区语义自此纯净：worker 无任何合法穿区理由 |
| P-7 | 中 | MCP 服务器 pending 永不连接，浏览器侦察铁律不可执行 | worker session init：`mcp_servers: [playwright: pending, tavily: pending]`，27 工具中 **0 个 mcp__\***；全程 curl/PowerShell | `.mcp.json` 已 scaffold 到 workdir，但 headless spawn 下 MCP 停在 pending；"不做浏览器侦察不准开始 curl"的铁律引用了一个不存在的工具，系统零感知（无 mcp 健康事件） | **实测定位（2026-09-14 对照实验，非猜测）**：`-p` 模式与交互模式 MCP 启动策略根本不同——交互式 CLI 自己等连接完成；`-p` 不等，init 带 pending 立即开工，把"等待"下放为模型可调用的 **`WaitForMcpServers` 工具**。实验：调它 → `ready:true` → playwright/chrome-devtools/tavily/fetch 全部出现；真跑 worker 3 轮从未调它（没人教，工具列表里也认不出来）。npx 暖启动仅 5s，与下载/网络/隔离配置无关 | **已实现（2026-09-14，未 commit 待拍板）**：侦察手册加第 0 步——"先调 WaitForMcpServers(timeout 30000)，失败/超时 → 本轮无浏览器，curl 起步"（等一次工具调用的成本 + 明确降级路径）；+1 回归测试（手册含等待指令/timeout/降级）；202 绿。可选后续：runner 解析 init 的 mcp_servers 状态发 `mcp_degraded` 事件（可观测性，暂缓） |
| P-8 | 低 | skills 交付靠目录偶然性 | `src/driver.py:254`（`scaffold.expand(root, eng)` 未传 `skills_src`） | 26 个攻击 skill 可见纯因 engagement 恰好放在 `D:\Downloads\hacker\` 子树下（CLI 沿父目录捡到 `.claude/skills`）；engagement 挪走即失效 | scaffold 支持 skills_src 参数但 driver 从不传 | **已实现（2026-09-14，未 commit 待拍板）**：先实测确认 `-p` 模式技能机制——`<cwd>/.claude/skills/<名>/SKILL.md`（frontmatter name+description）放进去即加载，模型可 `Skill` 工具直接调用，隔离 CLAUDE_CONFIG_DIR 不影响（两个对照实验）。修复=driver 一行传 `eng.get("skills_src")`（engagement.json 可选字段，用户拍板：以后 engagement 都跑在指定工作目录，不能依赖地理运气）+ dry-run 回归测试；usc-fresh engagement.json 已补字段；203 绿 |
| P-9 | ~~低~~ **非问题（2026-09-14 拍板）** | worker 越界外联获取工具——**guard 按设计工作，无需修复** | guard_violation：github.com ×2（Invoke-WebRequest 下 ysoserial，双源 404 后转本地手写 JDK21 工具链）、192.168.3.46 ×1（OOB 回连监听器=本机局域网 IP）、127.0.0.1 ×1（5000 开放代理测试跳板） | scope 外访问真实发生，guard 全部如实记录（机制诚实的预期行为）；worker 需求正当（后勤补给/OOB 技术动作），行为合理 | 无——摄像头拍到正常行为。曾考虑"本地弹药库/资源白名单"等修法，属过度设计，否决 | **结案：非问题**。本条保留作 guard 录像功能正常工作的实证 |
| P-10 | 低 | worker 自报轮次错乱 | r2 写 evidence 标"round 3"、r3 开工自述"第 4 轮"（均比实际多 1） | 次要；轮次以系统账为准 | worker 对当前轮号感知弱 | 观察 |
| P-11 | 中（停机语义） | CONTROL stop 在轮中触发只杀 worker 不停 run | `src/driver.py` on_heartbeat：`_poll_control` 消费文件后仅 `proc.kill()`，不置终止标志 | 单次 CONTROL stop：文件被心跳消费 → 当前轮被杀 → 收割后循环继续 → 下一轮轮界 poll 拿到 None → **新会话照开**。要停需写两次。设计§4.2"kill 当前 worker 走优雅停"与实现不符 | 心跳路径缺 break/终止标志传递 | **已修复 v2（2026-09-14，参照 ARTEX 具名取消/Cairn 即时杀，用户拍板停机语义=杀 worker+盘上状态全留+续跑=新会话接力）**：① 盯梢线程 0.5s 轮询 CONTROL，看到 stop 即 `controller_kill`（打 `_controller_kill` 死因标签 + taskkill /T 进程树杀）② 死因标签让 runner 把击杀归为**终态不续跑**（首版实机翻车：击杀被续跑梯子当瞬态故障 `--resume` 复活，worker 满血继续——正是这个标签堵的洞）③ Cairn 式 cancel-before-attach 竞态防护（信号早于 spawn → 线程盯到 proc 出现补杀）④ 心跳查岗职责移交 watcher；ARTEX 式具名原因进 run_end（stop_text）。**实机终验：写牌→worker 死 1 秒、零复活、收割/落盘/ledger_synced 全走完、run_end 带具名 stop_text**；214 绿 |
| P-12 | 低 | 被杀轮次的 usage 记账丢失 | r2 session_end：`stop_reason=timeout, tokens=0` | bigmodel usage 只在 result 事件里，时间盒击杀拿不到 result → 该轮真实消耗（估计几十万 token）不入账 | runner 击杀路径无 usage 估算兜底 | 观察 |

## 二、设计权衡观察（不算 bug，记录副作用）

| # | 观察 | 位置 | 副作用 | 本 run 影响 |
|---|------|------|--------|------------|
| O-1 | 端点事实 URL→path 归一化剥掉 host | `board.py:160-165` | 多目标 engagement 端点混流；单目标无影响 | 无 |
| O-2 | worker 可见 26 skill 但全程 0 次 Skill 调用 | transcript 0 命中 | Shiro-550 属模型内置知识，skill 价值在冷门面；路由表"按信号加载"无强触发 | 无减损，skills 实效待更冷门目标验证 |
| O-3 | 外部搜索能力同样死于 P-7（tavily pending） | 同 P-7 | worker 全程纯本地知识，无网络检索 | 本目标无碍 |
| O-4 | 黑板 stage 落盘滞后一轮 | driver 轮末顺序：bb.save() 在 check_goal() 之前 | 盘上 stage 永远是上一轮的（本次硬停后盘上 identity、实际已推进 exploit）；崩溃恢复时 check_goal 会重推，损害趋零 | 无实害，记录在案 |

## 三、待观察项（终态回填）

- [x] chains 边库：全程 0 边（无 findings + 观察者未跑，三挂点全空转——归因 P-3）
- [x] 观察者首个真实判定：**未发生**（三轮零 FINDINGS，DeepSeek 侧全程静默）
- [x] stoploss：三轮无触发（r1/r2 会话正常收场）
- [x] TERMINAL_C 路径：未走到（无 confirmed，且外部硬停）
- [x] writeback 三件：未触发（硬停跳过收尾；漏洞表/攻击面刷新/prior-intel-draft 均未落盘）
- [x] R2/R3 的 Shiro-550 战役：未出结果即被杀（r2 时间盒击杀、r3 外部硬停）；本地 OOB listener 未收到确认回连；.auto/FACTS 存 18 行（含 r3 未收割增量，可续跑）

## 四、健康机制简录（对照背景）

| 机制 | 实测表现 |
|------|---------|
| **被杀轮次数据不丢（终验）** | r3 被外部硬杀前写入 FINDINGS 的 F-001（Docker Registry v2 未授权：5000=/v2/_catalog 枚举 100 仓库），25 分钟后由独立冒烟 run 经 ID 去重补收割 → transcript 对账通过 → DeepSeek 真通道判 confirmed → status.md 落表（高） |
| **接力闭环** | r1 产 D-001（Shiro-550 RCE）→ r2 武器化（手写 ShiroPoc.java/shiro-fire.py/oob-listener.py/key 候选表）→ r3 开工先读上轮弹药库继续打——三轮接力零记忆丢失 |
| **合成交接** | r2 被时间盒击杀 → 控制器从磁盘状态自动拼 Handoff（origin=synthesized），r3 无感续接 |
| **worker 弹性** | github 两源 404 → 完全离线自建 JDK21 工具链，不空转 |
| 被动抽取 | 84 facts（全 kind 命中，含 r2 增量） |
| 事实衰减 | r1 抽验 51 条、observed→inferred 降级 2 条 |
| STATE.md 触达 | worker 主动 Read ×2（T3 双触达成真）+ 主动维护 DIRECTIONS |
| js-intel | worker 主动调用，产物在 .auto/out/ |
| 人类台账 | state/log.jsonl 8 条规范记录（经 P-5 盲区写入） |
| 阶段推进 | recon→identity→exploit 全部自动走通（r1/r2 轮次兜底生效） |
| guard 记录 | 4 次 scope 越界全部在案（P-5 盲区那笔除外） |
| r1 真实 usage | in 86,378 / out 18,006 / cache_read 695,232 / $1.23 / 32 turns / end_turn |

---

## 五、验收结论

**总评（2026-09-14 修复批后）：worker 层超预期；登记的全部可修问题（P-1~P-8 + P-11，共 8 个）已修复完毕，框架管道与防线闭环。测试 195 → 207 绿。P-9 经拍板结案为非问题；P-10/P-12/O-1~O-4 观察结案。**

**修复进度（全部未 commit，待拍板）：**

| 批次 | 修复 | 要点 | 测试 |
|---|---|---|---|
| 1 | P-3 观察者每轮跑 | judge 留 findings 分支 + 0-finding 轮独立 observe_session；prompt 两句（空发现轮指令/批注防抖） | +1（196 绿）|
| 2 | P-4 阴性升格 + FACTS 收紧 | merge_directions 内 done→immune 机械升格（幂等/有 finding 不误标）；prompt 三处：FACTS 只装正向情报、阴性走 DIRECTIONS done+note（参照 hxbai：正向进 knowledge/死路独立存储） | +2（198 绿）|
| 3 | P-5 guard 双缝 | 分隔符 `[/\\]` + .NET 静态写方法词表（实锤命令穿的是两道缝） | +1（199 绿）|
| 4 | P-6 台账归位（方案 A） | 纪律层/侦察手册台账改当前目录；侦察手册撤 status.md 手写指令；writeback 收尾搬运 `.auto/log.jsonl`→`state/log.jsonl`（去重幂等）；禁区自此无合法穿区理由 | +2（201 绿）|
| 5 | P-7 MCP 等待指令 | **根因实测**：`-p` 模式 CLI 不等 MCP，把等待下放为 `WaitForMcpServers` 工具，模型没人教不调 → 浏览器工具永不出现。修复=侦察手册加第 0 步（调 WaitForMcpServers timeout 30000，失败 curl 起步降级） | +1（202 绿）|
| 6 | P-8 skills 随工作目录走 | **机制实测**：`-p` 下 `<cwd>/.claude/skills/<名>/SKILL.md` 即放即用、可被 Skill 工具调用、隔离配置无碍。修复=driver 传 `eng.get("skills_src")`（engagement.json 可选字段——用户拍板 engagement 以后都跑指定工作目录）；usc-fresh engagement 已补字段 | +1（203 绿）|
| 7 | P-11 停机语义 v2 | **首版（查岗标志）实机翻车**：watcher 击杀被续跑梯子当瞬态故障 `--resume` 复活。**终版（参照 ARTEX/Cairn）**：盯梢线程 0.5s 轮询 CONTROL → `controller_kill` 死因标签 + taskkill /T 树杀 → 标签=终态不续跑；Cairn 式 cancel-before-attach 竞态防护；具名 stop_text 进 run_end。**实机终验：写牌→1 秒停、零复活、数据全留**；语义=杀 worker+盘上全留+续跑=新会话接力（用户拍板） | +6（214 绿）|
| 8 | P-1/P-2 watch 修复+CLI 冒烟 | `import json` 一行；新增 `tests/test_cli.py`（watch 完整 CLI 路径含坏行容忍/缺文件 rc=2/run 拒启 rc=2）；真 CLI 验证：当初崩溃的原命令完整回放全 run | +3（207 绿，先行合入 v2）|

**待修队列**：空——登记问题全部处置完毕（8 修 / 1 判非 / 5 观察）。

## 六、实机验收 run（2026-09-14 第二场，usc-jiuye，修复批生效后）

> 目标：jiuye.usc.edu.cn（新目标、engagement 故意放 `D:\at1-run\`（hacker 目录树外））；budget 1200s；3 轮；0 confirmed（worker 全程守纪律，阴性全走 FACTS/DIRECTIONS）。

| 修复项 | 实机验证结果 |
|---|---|
| P-8 skills 随包 | ✅ engagement 在 hacker 树外，技能照常可用（唯一供给=skills_src 字段） |
| P-7 MCP 等待 | ✅ **worker 主动调 WaitForMcpServers**（手册第 0 步教学生效；init 时 mcp 工具 0 个=pending 契约正常） |
| P-3 观察者每轮跑 | ✅ r1/r2 轮末均有观察者耗时（69s/约3min 的 harvest→handoff 空隙即 DeepSeek 调用）；无 finding 轮治理不再静默 |
| P-4 done→immune 升格 | ✅ **活收割升格 4 条**（D-003/D-004/D-005 真阴性 + D-001），note 原文保留（"3306 握手成功但 8s 无 MySQL 问候包"级别的细节直接进阴性清单）；blocked（D-007/D-009）正确不升格 |
| P-6 台账搬运 | ✅ `ledger_synced: appended 11`——.auto/log.jsonl 收尾自动归档 state/log.jsonl |
| writeback 三件 | ✅ prior-intel-draft 生成 / status.md 攻击面刷新（7 行）/ 漏洞表空（无 confirmed，正确） |
| 事实衰减 | ✅ 74 抽验 / 34 降级 / 1 自愈（终轮窗口小导致批量降级——机制按设计工作，窗口偏差记 O-5） |
| 合成交接 | ✅ r3 被预算截杀 → 代码合成 Handoff |
| P-11 停机语义 | ✅ **专场上机终验通过（2026-09-14 第二场专测）**：run 进行中写 CONTROL → **1 秒** worker 死（controller_kill 终态）→ 零复活 → 收割/事实衰减/方向合并/合成 Handoff/ledger 同步全部走完 → `run_end control-stop(D)` 带具名 stop_text。首验曾翻车（击杀被续跑梯子复活），死因标签修复后复验通过 |

**O-5（新观察）**：事实衰减以"最近一轮窗口"为基准——终轮窗口极小（被预算截杀）时批量降级（34/74），下轮以 inferred 重验自愈。机制按设计，但轮窗偏差值得留意。
**附加发现**：D-004 note 提示 `/source/preview/cid` 无效 cid 空壳页泄漏企业端菜单、"人才搜索/生源速"为真正敏感面——后续 engagement 的线索（本轮 scope 内未深挖）。

**验收快照：**
- **成立且有效的**（真实数据锻炼过）：黑板+被动抽取、轮末收割（ID 去重/offset/merge）、STATE.md 投影+触达、方向层接力、Handoff 双路（模型版+合成兜底）、事实衰减、guard 记录、阶段自动推进、dry-run 门、时间盒击杀、**被杀轮次数据补收割（F-001 终验）**。
- **修复后才可用/待下次真跑验证**：观察者 0-finding 轮输出质量（P-3 的 DeepSeek 真通道行为）、阴性清单对 worker 防重复的实际效果（P-4）、guard Windows 形态告警（P-5）。
- **未被真实锻炼的**：chains 图（等首个 finding 后的观察者判重）、skills（P-8 交付偶然 + O-2 未触发）、TERMINAL_C、stoploss。
- **worker 纪律质量**：0 垃圾 FINDINGS、阴性全 inferred、即时落盘、方向表认真维护——C-1 纪律层在 glm-5.3-flash 上完全立得住。

**续跑提示**：本 engagement 可直接 `python -m src run` 续跑——.auto/FACTS 18 行增量会按 offset 续收，r3 未竟方向在 DIRECTIONS 里；修复批生效后，续跑还能顺带验证 P-3/P-4 的真通道行为。
