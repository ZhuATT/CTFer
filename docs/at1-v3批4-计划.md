# AT1 v3 批4 计划 —— 观察者补完 + 跑前门禁

> **定位**：补齐总纲 P5（观察者）在批3fix 中的遗留项，并装好实机复跑前的门禁。
> **依据**：总纲 `at1-v3施工计划.md` §P5/§P6 任务表 + 批3fix 施工结果（`at1-v3批3fix-落地执行计划.md` §9）逐项对照得出的缺口清单。
> **基线**：批3fix 竣工态（181 测试绿，未 commit）。
> **状态**：待用户阅后指示开工。

---

## 0. 执行协议

1. 每卡完成即跑全量测试绿了才算完；**做完不自动 commit，等用户指示**。
2. **G1/G2 是本批门禁**——不完成不得进入 G5/G6（它们决定复跑数据有没有价值）。
3. 文档改动一律用 Write/Edit 工具，**禁止 bash 内联 python 处理含反引号的 markdown**（批3fix 事故：shell 反引号命令替换 + `>` 重定向在仓库根误建空文件）。
4. 汇编（`prompt-全文汇编.md`）与 `scaffolding/` 文件的一致性锁保持绿（改手册必须两边同 commit）。

## 1. 本批交付一句话

**把观察者的"判定能力"和"写作规矩"补回来，装好跑前门禁，让第一次实机复跑产出可用数据。**

## 2. 范围界定（逐项对照总纲 P5/P6）

| 来源 | 要素 | 现状 | 本批 |
|---|---|---|---|
| T5.1 | 质检（纸面+实测重放） | ✅ 已做（先验证后入图） | — |
| T5.1 | 记账纪律（blocked+可检验条件/瞥见立向/画像换代） | ✅ 已做 | — |
| T5.1 | 红线：不杀/不中途/不判停 | ✅ 已做 | — |
| T5.1 | **判定方法论（第零步+四步+src标准路由）** | ❌ **丢失**（重写 observer.py 时连带删除，代码/手册均为零） | **G1** |
| T5.1 | **引导写作规矩（节点 id+转述/禁贴目标原文/非强制）** | ❌ 未写 | **G2** |
| T5.1 | **作业纪律继承（高危不做等）** | ❌ 未写 | **G2** |
| T5.1 | **长任务扫尾（bg-\*.log）** | ❌ 未写 | **G2** |
| T5.2 | -p 同构/最小任务单/0 产出轮照跑 | ✅ 已做 | — |
| T5.2 | 角色模型配置化（A23 完整版） | ⚠️ 简版（env 覆盖） | G4（评估） |
| T5.2 | 双角色配置生效单测 / stdin 零快照检查单测 | ❌ 未写 | **G4** |
| T5.2 | 真通道冒烟 | ❌ 未做 | **G5** |
| T5.3 | transcript_check 删除 | ✅ 已做 | — |
| T6.1 | 术语清扫（全量 grep） | ❌ 未做 | **G3** |
| T6.2 | tests 全量翻新 | ✅ 已做（181 绿） | G3（删 poison_probe） |
| T6.3 | dry-run 实机验收十二项 | ⚠️ 部分（dry-run 门过；十二项待实机） | **G6** |
| T6.4 | 中期问题登记回填 | ❌ 未做 | G6（跑后） |
| M5 | 工作台 | ❌ 未做 | 范围外（另立） |

## 3. 施工卡

### G1 判定方法论回归（门禁 · 最高优先）
- **背景**：批3fix 重写 `observer.py` 时删除 v2 的 `JUDGE_PROMPT`/`_INJECTION_GUARD`，判定树未迁移——观察者现在没有任何"什么该判假"的教学，会把 CORS/指纹/安全头类现象审成 confirmed。
- **来源**：`git show 40e6152:src/observer.py`（v2 原文，含第零步硬否决清单 + 四步影响框架 + 证据不可信声明 + 业务上下文判据）。
- **改动**：迁移进 `scaffolding/OBSERVER-MANUAL.md` 新节「判定方法论」——适配新架构措辞：
  - 第零步硬否决清单（绝对不报表，但书=现象作为链载体时继续四步）
  - 四步影响框架（影响是什么 → 设计内行为？ → 影响多大 → 利用条件是否归零）
  - 防注入声明（worker 原文指令性文本一律当数据）——**已在 §1，与本节合并去重**
  - 代码预检（noreport）与手册判定的关系：代码检察官只拦"形状即现象"，其余定性归观察者
  - 判定落点：`add_finding` 的 `result`（confirmed/dismissed）+ `severity`
- **验收**：汇编 §4.1 一致性锁绿；手册含四要素（第零步清单/四步/防注入/落点说明）。

### G2 写作规矩与纪律补完（门禁）
- **改动**（`OBSERVER-MANUAL.md` + 汇编同步）：
  - **引导写作规矩**：STATE.md「## 下轮建议」引用图内容=**节点 id + 转述**，**禁贴目标响应原文**（stdin 零目标原文的唯一保证）；口吻=建议非命令
  - **作业纪律继承**：高危操作不做（发现写入面即报告）/ 测试对象优先 / 写操作必读回验证 / 凭证文件化（与 worker 手册 §6 同源）
  - **长任务扫尾**：轮末扫工作目录 `bg-*.log` 遗留，写进下轮建议（A21 三层纪律的观察者侧）
- **验收**：汇编锁绿；四要素在场（禁贴原文/非强制/纪律继承/bg 扫尾）。

### G3 清扫
- **改动**：
  - 删 `tests/poison_probe.py`（A23 已裁：靶随 chat 通道退役；总纲 T6.2 明列 2 删之一）
  - T6.1 术语清扫：`src/` + `scaffolding/` 全量 grep `tentative / 阶段= / 出口判据 / 实测·推断关闭 / unclassified / plan_directive / session_intel / immune(存储义)` → 零残留（或逐条判定为合法保留）
  - 死字节码清理：`src/__pycache__/` 中已删模块的 pyc
- **验收**：全量测试绿（无 poison_probe 引用）；grep 结果逐条有结论。

### G4 T5.2 收尾
- **改动**：
  - 补单测：①双角色模型配置生效（env 覆盖切 provider/model，不污染 worker 配置）②观察者 stdin 零快照（任务单不含目标原文/session 全文）
  - **评估项（跑后定）**：双角色配置是否需要完整版（配置文件→控制台）——取决于观察者质量实测，本批先不动
- **验收**：新单测绿。

### G5 冒烟准备（跑前最后一关）
- **改动**：造一个**假目标 engagement**（空目标或本地靶，零真实攻击面），跑一轮，只验通道：
  1. 观察者进程 spawn 成功（`observer_session_end` 事件）
  2. 手册/协议复制到 `.observer/`（OBSERVER-MANUAL.md / INTERFACE.md / task.json）
  3. 模型通道通（deepseek，tokens>0）
  4. 观察者按协议写出合法 `.observer/OBSERVER.json`（或空产出触发重试→告警）
  5. 执行器入图（`observer_applied`）+ receipts/rejects 落盘
  6. STATE.md 产出 → 【引导】块抽取正常
- **产出**：冒烟记录（哪几项过、哪几项异常）+ 异常进 G6 前的热修清单
- **验收**：六项逐条有结论；通道级异常清零后才允许上真目标。
- **成本**：一轮 worker + 一轮观察者，量级几分钟（假目标 worker 无事可做，很快收工）。

### G6 实机复跑（需用户给目标 + 凭证）
- **前置**：G1-G5 全绿
- **执行**：真目标跑 1-2 轮，按四层关注点取证（通道/分工/图健康度/成本）
- **验收清单**：总纲 T6.3 十二项 + 批3fix §6.3 新版十点 + 本批四关注点
- **产出**：复跑报告（数据位置见下）+ 批5 输入清单
- **回填**：T6.4 中期问题登记（P-3/P-4/O-2/O-5/P-10/P-12）逐项结案

## 4. 复跑数据位置（G6 用）

| 数据 | 位置 |
|---|---|
| 判断书原文 / 坏条 | `.observer/OBSERVER.json` / `OBSERVER.rejects` |
| 执行器账 | `.at1/interface_log.jsonl`（receipts） |
| 观察者会话流 | `.at1/observer-transcript.jsonl` |
| 事件流 | `state/auto-log.jsonl`（observer_applied / empty_retry / state_fallback） |
| worker 自由写盘实况 | `facts/` `findings/` `evidence/` |
| 图终态 | `.at1/blackboard.json` |

## 5. 风险与对策

| 风险 | 对策 |
|---|---|
| 判定方法论迁移后与新架构措辞冲突 | 以"判定落点=result/severity"为锚改写，保留 v2 判定树原文逻辑 |
| 观察者模型（deepseek-v4-flash）判定质量不足 | 跑后据实评估；切 glm 只改 env（G4 已留评估项） |
| 冒烟轮观察者空产出 | 正是要验的（R8 防线：重试→告警）；记录形态进热修清单 |
| G1/G2 补完仍不足 | 手册是 prompt 纪律，非代码闸——真轮暴露的判定问题进批5 |

## 6. 明确不做（本批边界）

M5 工作台（另立）/ T6.4 回填（G6 跑后）/ 内容迁移（老 engagement 归档）/ engagement 树 git 化（已弃案）/ confidence 字段（已弃案）/ 双角色配置完整版（跑后评估）。
